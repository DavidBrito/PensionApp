<h3> Propriedades </h4>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Nome</th>
      <th>CEP</th>
      <th>Logradouro</th>
      <th>Número</th>
      <th>Cidade</th>
      <th>Estado</th>
      <th>Proprietário</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @properties.each do |property| %>
      <tr id="properties-values">
        <td id="id"><%= property.id %></td>
        <td id="name"><%= property.name %></td>
        <td id="postal_code"><%= property.postal_code %></td>
        <td id="proprietary_name"><%= property.address %></td>
        <td id="number"><%= property.number %></td>
        <td id="city"><%= property.city %></td>
        <td id="state"><%= property.state %></td>
        <td id="proprietary_name"><%= find_user_name(property.proprietary_id) %></td>
        
        <td><%= link_to 'Editar', edit_property_path(property), id: 'edit' %></td>
        <td><%= link_to 'Deletar', property, method: :delete, data: { confirm: 'Tem certeza?' }, id: 'destroy' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3>Quartos</h3>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Número</th>
      <th>Propriedade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr id="rooms-values">
        <td id='id'><%= room.id %></td>
        <td id='number'><%= room.number %></td>
        <td id='property-name'><%= find_property_name(room.property_id) %></td>
        <td><%= link_to 'Editar', edit_room_path(room), id: 'edit' %></td>
        <td><%= link_to 'Delete', room, method: :delete, data: { confirm: 'Tem certeza?' }, id: 'delete' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3>Vagas</h3>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Valor</th>
      <th>Estado</th>
      <th>Quarto</th>
      <th>Ocupante</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vacancies.each do |vacancy| %>
      <tr id='vacancies-values'>
        <td id='id'><%= vacancy.id %></td>
        <td id='value'><%= vacancy.value %></td>
        <td id='status'><%= vacancy.status %></td>
        <td id='room-id'><%= vacancy.room_id %></td>
        <td id='user-name'><%= find_user_name(vacancy.vacancy_owner_id) %></td>
        
        <td><%= link_to 'Editar', edit_vacancy_path(vacancy), id: 'edit' %></td>
        <td><%= link_to 'Deletar', vacancy, method: :delete, data: { confirm: 'Tem certeza?' }, id: 'delete' %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>
<%= link_to 'Nova Propriedade', new_property_path %>
<br>
<%= link_to 'Novo Quarto', new_room_path %>
<br>
<%= link_to 'Nova Vaga', new_vacancy_path %>
<br>
<%= link_to 'Voltar', proprietary_path %>


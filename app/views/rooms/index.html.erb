<h1>Listing Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Número</th>
      <th>Nome propriedade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td id='id'><%= room.id %></td> 
        <td id='number'><%= room.number %></td>
        <td id='property_name'><%= find_property_name(room.property_id) %></td>
        
        <td><%= link_to 'Editar', edit_room_path(room), id: 'edit' %></td>
        <td><%= link_to 'Deletar', room, method: :delete, data: { confirm: 'Tem certeza?' }, id: 'delete' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Quarto', new_room_path %>
